---
description: Build my AI Partner Method personal authority website to £10k quality
---

You are about to build the student a personal authority website that meets the £10k quality bar.

**Read these files in order before doing anything:**

1. `CLAUDE.md` — your operating manual
2. `starter/index.html` — the £10k reference build (the floor — every site starts here)
3. `starter/style.css` — design tokens, motion choreography, micro-details
4. `starter/script.js` — sticky nav, reveal observer, carousel, a11y
5. `design-reference/quality-bar.md` — pre-ship checklist (run before shipping)
6. `design-reference/brand-dna.md` — visual language
7. `design-reference/sections.md` — section patterns + deployment
8. `design-reference/copywriting.md` — copy formulas
9. `design-reference/color-palettes.md` — accent options
10. `design-reference/typography.md` — type system

Then follow the build workflow in `CLAUDE.md` step by step:

1. Greet + confirm readiness
2. Run the 7-question interview (one question at a time)
3. Confirm answers back to the student
4. Copy `starter/*` to the repo root
5. Customise the 8 things listed in `CLAUDE.md` Step 5 (name, headline, lead, stats, qualifier, FAQ, socials, accent colour, photo)
6. Run the `quality-bar.md` checklist — every box must be ✓
7. Preview locally
8. Iterate based on feedback
9. Deploy to Vercel using the workflow in `design-reference/sections.md`

**Critical rules:**
- Don't skip the interview
- Don't ask technical questions
- Don't fabricate testimonials — keep the `[Client Name]` placeholders with the REPLACE comments
- Don't regress on the starter's structure, motion, or CSS architecture
- The starter is the FLOOR — every site must meet or exceed it
- Use the brand DNA strictly (white bg, near-black text, single accent, Inter font, 8px radius — never pill)
- Build at the **repo root**, not in `starter/` (that's the reference, not the destination)
- One file per type at the root: `index.html`, `style.css`, `script.js`
- After building, run the £10k quality bar checklist BEFORE showing the student
- After confirming quality, ask before deploying — they may want to iterate first

Start now: read `CLAUDE.md` first, then begin the workflow.
